// Proxy entry point for Android builds
// This file exists inside the app/ project root so Vite can resolve it.
// The actual Android entry point lives outside the project root at ../android/src/entry.tsx.
import "@android/entry"
