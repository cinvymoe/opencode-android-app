// Resolution proxy for Android source files
// Used by the opencode-android:entry Vite plugin as a resolution anchor
// so that bare imports from ../android/src/ files resolve correctly
// relative to the app/src directory (e.g., node_modules and aliases).
export {}
